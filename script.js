var main = function (input) {
  var names = [];
  var main = function (input) {
    var found = false;

    //loop over names and set found to true if the input name already exist
    var index = 0;

    for (var index = 0; index < names.length; index += 1) {
      var currentName = names[index];
      if (currentName == input) {
        found = true;
      }
    }
  };
};
